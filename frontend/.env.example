# Frontend production environment variables (set in Vercel dashboard, not committed)
# Must be set at BUILD time (Next.js inlines NEXT_PUBLIC_* vars)
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
