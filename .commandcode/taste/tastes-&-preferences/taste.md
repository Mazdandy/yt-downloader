# Tastes & Preferences
- Communicates in Indonesian (Bahasa Indonesia) and expects the agent to reply in the same language — though on occasion writes casual, typo-heavy English instead (e.g., "i think the issue is when in downloading phase, it took too long, how it happen?"), so language may vary by session. Confidence: 0.9
- Tends to give terse, minimal-context instructions — bug reports (e.g., just raw error text like "i got 422", "spawn yt-dlp ENOENT", a bare "404", or a vague "seems like yt-dlp is not working well") and one-line change requests alike (e.g., "hapus rate limit", "ubah semua nama dari Vibe Downloader menjadi Cloud Downloader") — and relies on the agent to investigate, reproduce, root-cause, and scope the change across the whole repo (source, config, env examples, deploy manifests, docs, lockfiles, mockups, tool settings) before fixing. Confidence: 0.99
- Prefers backend-first implementation order when building full-stack features (explicitly asked for the backend before the frontend). Confidence: 0.7
- Welcomes the agent asking clarifying questions while coding, not just up-front ("maybe you can ask me while you code"). Confidence: 0.8
- Expects implementation to be driven from the PRD/spec documents in the repo (referenced a PRD markdown file as the definition of what to build). Confidence: 0.6
- Has their own VPS and prefers self-hosting there over split PaaS deploys (Vercel+Railway). When offered Docker vs no-Docker on the VPS, explicitly chose the no-Docker/systemd setup ("yang gapake docker dong") — favors the simplest option with the least extra tooling. Confidence: 0.8
- Prefers installing CLI tools without Homebrew on their local Mac (asked how to get yt-dlp and ffmpeg "without brew") — favors direct binary downloads, static builds, or pip over installing another package manager. Confidence: 0.6
- Fact-checks the agent's investigation claims by asking which specific commands/flags were used (e.g., "did u use --list-formats?") and pushes back with concrete counter-observations (e.g., "but the response said that we should use --list-formats") — expects the agent to verify against the actual code, distinguish tool-generated error text from real recommendations, and explain the trade-offs rather than assume. Confidence: 0.7
- Prefers interrupted/failed work to resume from where it left off rather than restart from scratch — asked explicitly for downloads to be continued, not re-run from zero ("bisa ga kalo misal gausah diulang, tp dilanjutkan") after learning retries restarted at 0%. Values not redoing already-completed work. Confidence: 0.8
- Responds to the agent's prioritized, numbered breakdown of root causes by selecting one item to fix (picked "nomer 2" from the six causes listed) — appreciates options ranked by impact and picks from them. Confidence: 0.6
- Reports regressions by anchoring on project history and recent code changes ("do you remember the first try...?", "since we have changes on that part, the download cant be done") — expects the agent to remember prior bugs/fixes discussed in earlier sessions and to start the investigation from the recent commits/git history. Confidence: 0.65
- Treats the mockups/designs in `stitch_prd_visual_design/` as the source of truth for the frontend look — when the UI drifts from those approved mockups, reports it as a regression ("layoutnya jadi jelek lagi, tadi udah mirip yang di folder stich") and expects the agent to diff the current UI against the stitch mockups (and the git history that changed them) and restore fidelity, not just patch symptoms. Confidence: 0.65
- Wants changes verified/tested on the local dev setup before considering them done or deploying ("bisa test di local dulu?") — prefers a local end-to-end run as the first validation step rather than going straight to the VPS/production. Confidence: 0.65
- Works with a main-branch workflow: asks for finished work to be committed and pushed straight to main ("push ke main") rather than using feature branches or pull requests. Confidence: 0.5
- When diagnosing "why does the live site look wrong", distinguishes between the deployed artifact and the local code by fetching the live site and comparing (deployed site was still an old pre-refactor build while local was already Tailwind/stitch) — root-causes to "deploy hasn't been updated", then commits/pushes local changes and hands the user exact VPS commands (`git pull` + build + `install-services.sh`) to run themselves rather than insisting on SSH access. Confidence: 0.7
- Has `NODE_ENV=production` set in their shell environment (confirmed explicitly: "i use production env"), which affects every npm command they run locally — it breaks `next dev` (PostCSS/Tailwind skipped → `@tailwind` parse errors in globals.css even though `next build` works) and makes `npm ci` prune devDependencies (typescript), so `tsc` vanishes after install. npm scripts must therefore be robust to the shell env: pin NODE_ENV explicitly (`NODE_ENV=development next dev`) or explicitly include dev deps in build steps (`npm ci --include=dev && tsc`, e.g. a `build:prod` script) instead of relying on the ambient NODE_ENV. Confidence: 0.85
- When handed deploy commands to run themselves on the VPS, asks for the rationale behind each step/flag before executing (e.g., "oh jadi sekarang kalo mau deploy harus include node env?") — expects the agent to explain why the NODE_ENV prefixes/`env -u NODE_ENV` are needed during install and build, not just give commands. Confidence: 0.5
- Values documentation staying in sync with the actual code: asked whether the deploy README was still accurate ("readme untuk deploy masih sama?") and expects the agent to verify README claims against the real files (deploy scripts, systemd units, Caddyfile, docker-compose, config) rather than trusting the docs at face value; when the agent flags stale docs and proposes syncing them, the user readily approves ("ya") — and expects the sync to cover config tables, design-decision sections, and supporting files like `.env.example`, not just the README. Also proactively asks that newly-explained operational rationale (e.g., why `NODE_ENV=development` is required during install/build and what `env -u NODE_ENV` does) be written into the README ("tambahkan ke readme dong") so the knowledge persists beyond the chat. Confidence: 0.8
